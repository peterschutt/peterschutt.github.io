# PS, codes

My personal blog/site/whatever.

## Pre-commit

Setup hooks:

`$ pre-commit install`

Run against all files:

`$ pre-commit run --all-files`

Update all hooks:

`$ pre-commit autoupdate`
